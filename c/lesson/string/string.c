#include <stdio.h>
#include <string.h>

int main(int argc, char* argv[])
{
    char str[128];

    /* memset(&str[0], 0, 128); */

    /*
    str[0] = 'a';
    str[1] = 'b';
    */

    /*
    str[3] = 'd';
    str[4] = 'e';
    */

    printf("%s\n",str);

    return 0;
}
